{
  "name": "kg-events",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "npm run test:persistence",
    "test:persistence": "rm -rf .test-dist && tsc -p tsconfig.test.json && node --experimental-specifier-resolution=node .test-dist/persistence/PersistenceRegression.test.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
