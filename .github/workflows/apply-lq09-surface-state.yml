name: Apply LQ-09 surface state

on:
  push:
    branches:
      - fix/lq08-12-convergence

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/lq08-12-convergence
      - name: Apply LQ-09 surface state convergence
        env:
          ACTOR: ${{ github.actor }}
        run: |
          if [ "$ACTOR" = "github-actions[bot]" ]; then
            exit 0
          fi
          python3 scripts/apply-lq09-surface-state.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/ui/KnowledgeSurfaceState.ts src/ui/app.ts src/ui/panels/PanelController.ts scripts/verify-knowledge-surface-state.mjs package.json
          git commit -m "Centralize knowledge surface navigation state"
          git push origin HEAD:fix/lq08-12-convergence
