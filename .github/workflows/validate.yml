name: Apply LQ-04 regression convergence

on:
  push:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/cache-knowledge-relations
          fetch-depth: 0
      - name: Apply relation regression convergence
        run: python scripts/apply-lq04-regression-fix.py
      - name: Restore validation workflow and commit correction
        run: |
          git show 0374d9e9b608d4d4745402017609e3f0ccdda178:.github/workflows/validate.yml > .github/workflows/validate.yml
          rm scripts/apply-lq04-regression-fix.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Converge node-detail relation regression on indexed authority"
          git push origin HEAD:fix/cache-knowledge-relations
