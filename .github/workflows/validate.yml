name: Apply LQ-04 mobile correction

on:
  push:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/cache-knowledge-relations
          fetch-depth: 0
      - name: Apply mobile relation-cache correction
        run: python scripts/apply-lq04-mobile.py
      - name: Restore validation workflow and commit correction
        run: |
          git show 0374d9e9b608d4d4745402017609e3f0ccdda178:.github/workflows/validate.yml > .github/workflows/validate.yml
          rm scripts/apply-lq04-mobile.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Keep mobile relation cache on stable graph generation"
          git push origin HEAD:fix/cache-knowledge-relations
